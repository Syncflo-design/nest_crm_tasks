[project]
name = "nest_crm_tasks"
authors = [{ name = "NestERP", email = "info@nesterp.com" }]
description = "Lead Activity Hub - custom CRM task management for NestERP clients"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []
license = { text = "MIT" }

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "nest_crm_tasks"
